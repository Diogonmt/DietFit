<Page
    x:Name="pageRoot"
    x:Class="DietFit.Views.Consulta"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DietFit.Views"
    xmlns:common="using:DietFit.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="Fill" ImageSource="ms-appx:///Assets/fundo.png"/>
        </Grid.Background>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Rectangle Fill="Black" Height="140" Stroke="Black" VerticalAlignment="Top"/>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="Consulta de Nutrição" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="107,79,0,0" Grid.Row="1" TextWrapping="Wrap" Text="A sua consulta foi marcada para o dia " VerticalAlignment="Top" FontWeight="Bold" FontSize="18.667" Foreground="Black"/>
        <TextBlock x:Name="textBlock1" HorizontalAlignment="Left" Margin="107,168,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Se a data não for do seu agrado dirija-se á página de contacto com o nutricionista e avise o quanto antes, para a consulta ser adiada" VerticalAlignment="Top" FontSize="18.667" FontWeight="Bold" Foreground="Black"/>
        <TextBlock x:Name="textBlock2" HorizontalAlignment="Left" Margin="466,79,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" FontSize="18.667" Foreground="Black"/>
        <TextBlock x:Name="textBlock3" HorizontalAlignment="Left" Margin="548,79,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" FontSize="18.667" Foreground="Black"/>
        <TextBlock x:Name="textBlock4" HorizontalAlignment="Left" Margin="630,79,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" FontSize="18.667" Foreground="Black"/>
        <TextBlock x:Name="textBlock5" HorizontalAlignment="Left" Margin="794,79,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" FontSize="18.667" Foreground="Black"/>
        <TextBlock x:Name="textBlock6" HorizontalAlignment="Left" Margin="885,79,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" FontSize="18.667" Foreground="#FF0C0C0C"/>
        <TextBlock x:Name="textBlock7" HorizontalAlignment="Left" Margin="727,79,0,0" Grid.Row="1" TextWrapping="Wrap" Text="pelas" VerticalAlignment="Top" FontSize="18.667" RenderTransformOrigin="0.419,-0.136" Foreground="Black"/>
    </Grid>
</Page>
